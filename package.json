{
  "name": "dns01-stack",
  "private": true,
  "scripts": {
    "docker:build": "docker compose -f docker-compose.build.yml build",
    "docker:push": "docker compose -f docker-compose.push.yml build --no-cache --push"
  }
}
