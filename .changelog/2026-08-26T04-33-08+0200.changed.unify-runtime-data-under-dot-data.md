Unify runtime data under `.data/server` (acme-dns config + SQLite) and `.data/client` (Nuxt plugin files). Dev keeps PEMs in `.data/letsencrypt`; production mounts host `./data/{server,client,letsencrypt}` with PEMs at `/etc/letsencrypt`.
