Ship `seed/config.cfg` into the production image as `/app/config.cfg.default` and remove the embedded `defaultConfig.ts` fallback, so first-boot Docker seeding uses the same :53 / :80 template you edit in seed.
