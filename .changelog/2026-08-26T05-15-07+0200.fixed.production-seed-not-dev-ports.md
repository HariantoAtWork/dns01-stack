Fix production first-boot seeding: prefer `seed/server/config.cfg` (DNS :53, API :80) over the baked `config.dev.cfg` runtime default, set `DNS01_CONFIG_DEFAULT` in the image, and omit the dev template from the runner.
