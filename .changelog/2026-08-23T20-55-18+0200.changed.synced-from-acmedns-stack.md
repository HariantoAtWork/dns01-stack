Synced certificate download and certs UI polish from acmedns-stack development repo.

Ported:
- Live certificate zip download API (`/api/certs/download/[name]`) with zipStore helper
- Shared client download utility (used by certs and clientstorage)
- Certs page: separate Download button, actions menu for delete, trash confirm dialog, coloured Last Saved / Trash nav pills
- `liveOnDisk` flag on cert status entries

DNS01 branding preserved (`dns01-stack`, `DNS01 Stack`, `dns01-client`).
