Add a Domain-panel checkbox that verifies stored acme-dns credentials still authenticate on the account’s server_url (local DB or remote /update probe), so wiped or reset servers show as invalid without writing a TXT record.
