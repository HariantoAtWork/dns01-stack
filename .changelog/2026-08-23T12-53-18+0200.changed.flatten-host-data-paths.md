Flatten host bind paths from nested `data/dns01/{config,data,host}` to sibling folders `data/dns01_config`, `data/dns01_data`, and `data/dns01_host`. Named Docker volumes and in-container paths (including `/app/data/dns01/backups`) are unchanged.
