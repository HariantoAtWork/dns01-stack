The Register form default in compose now follows `ACMEDNS_URL` unless `NUXT_PUBLIC_DEFAULT_ACMEDNS_URL` is set, matching the development stack and `.env.example`. App code was already in sync after the cert-download and seed-config ports.
