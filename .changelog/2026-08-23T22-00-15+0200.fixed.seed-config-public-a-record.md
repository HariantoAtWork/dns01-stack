Use TEST-NET public IP `198.51.100.1` in the seed A record (not localhost) and restore Go-style operator comments so Let's Encrypt NS glue is clearly documented while local listen stays loopback for `bun run dev`.
