Point seed `database.connection` at `/var/lib/acme-dns/acme-dns.db` so production Compose’s `./data/dns01_data` bind actually receives the SQLite file instead of writing under ephemeral `./.data` in the container.
