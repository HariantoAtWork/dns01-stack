Reordered `environment:` blocks in `docker-compose.yml.example` and `.env.example` by type: system → backend → frontend, with section comment headers. Backend vars in `.env.example` follow the same logical order as compose.
