Rename `build/acmedns-nuxt` to `build/dns01-stack` and rebrand the operator UI to DNS01 Stack (titles, nav, session cookie, plugin module, logs, backups path). Keep acme-dns protocol paths, `ACMEDNS_*` env keys, and SQLite wire layout.
