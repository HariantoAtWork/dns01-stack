Replace bare `# system` / `# backend` / `# frontend` section headers in `.env.example` and `docker-compose.yml.example` with visible Unicode divider titles (System, Backend, Frontend) for easier scanning.
